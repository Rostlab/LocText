language: python
python:
  - "3.5"
install:
  - "pip install ."
before_script:
  - "pip install git+https://github.com/Rostlab/nalaf.git"
  - "python -m nalaf.download_corpora"
  - "pip install codecov"
after_success:
  - "codecov"
script:
  - "pytest"
branches:
  only:
    - master
    - develop
